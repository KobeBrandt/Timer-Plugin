haptics:
    knock:
      DEFAULT: knock
    ringing:
      DEFAULT: ringing
    jingle:
      DEFAULT: jingle
    buttonPress:
      DEFAULT: sharp_state_change
      MX Master 4: sharp_collision    # Device-specific mapping
